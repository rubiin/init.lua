# DOCS https://github.com/crate-ci/typos/blob/master/docs/reference.md
#───────────────────────────────────────────────────────────────────────────────

# ignore these words
[default.extend-words]
noice = "noice" # plugin name
iterm = "iterm" # iterm

# FIX: this config doesn work for some reason , check
# ignore these files
# INFO (needs glob due to conform.nvim appending stuff for temporary file)
[files]
extend-exclude = ["*spellfixes.lua"]
